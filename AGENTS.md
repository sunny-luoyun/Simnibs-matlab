# 项目指令

## 推送规则
- 用户说 "push" 或 "推送" 时，需要同时推送到两个远程:
  1. GitHub (origin, git@github.com:sunny-luoyun/Simnibs-matlab.git)
  2. Gitee (git@gitee.com:luoyun-weixi/simnibs-matlab.git)
- 方法: `git push origin <branch>`，因 origin 配置了两个 pushurl，会同时推送
